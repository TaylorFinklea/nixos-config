{ config, pkgs, ... }:
{
  # services.sketchybar = {
  #   enable = true;
  #   package = pkgs.sketchybar;
  # };
  # services = {
  #   tailscale = {
  #     enable = true;
  #     package = pkgs.tailscale;
  #   };
  # };
}
